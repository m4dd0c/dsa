#include <bits/stdc++.h>
using namespace std;

void print_arr(auto *arr, int size) {
  cout << endl;
  for (int i; i < size; i++) {
    cout << arr[i] << " ";
  }
  cout << endl;
}

void print_vect(auto *vect) {
  for (auto i : vect) {
    cout << i << " ";
  }
}